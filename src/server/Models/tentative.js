export default class Tentative {
    constructor(combinaison, timestamp, player) {
        this.combinaison = combinaison;
        this.timestamp = timestamp;
        this.playerName = player;
    }
}
